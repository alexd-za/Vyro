# secrets & local config
.env
.mcp.json

# python venv
.venv/
__pycache__/
*.pyc

# working artifacts (your footage / cuts / candidate lists)
work/
out/
**/candidates/
post_package.json
candidates.json
candidates.md

# inbox drop folder — ignore the videos, keep the folder
inbox/*
!inbox/.gitkeep
!inbox/README.txt
knowledge/inbox-manifest.json

# derived handoff snapshot (regenerate with ./clip handoff)
HANDOFF.md

# downloaded display fonts (regenerate with ./clip fx fonts)
assets/fonts/
